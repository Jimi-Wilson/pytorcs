# Pinned to match the sacpid-clean training environment.
# torcs_omnisafe_wrapper.py imports torch and omnisafe at module load time,
# so both are required even for PPO-only eval.
#
# CPU-only PyTorch (no CUDA):
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r eval/requirements.txt
#
# GPU / default PyTorch:
#   pip install -r eval/requirements.txt

torch==2.5.1
numpy==1.23.5
scipy==1.11.4
safety-gymnasium==1.0.0
omnisafe==0.5.0
gymnasium==0.28.1
stable-baselines3==2.3.2

# Eval tooling
rich>=13.0.0
flask>=3.0.0
